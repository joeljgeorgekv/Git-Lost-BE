"""Helper utilities for LangGraph components (placeholder)."""

from __future__ import annotations


def normalize_text(text: str) -> str:
    """Placeholder normalization helper."""
    return text.strip()
