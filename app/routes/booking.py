from fastapi import APIRouter

# This router is intentionally empty to follow the instruction of having only a single
# mock endpoint (/health). Add booking-related routes here in future iterations.
router = APIRouter(tags=["booking"]) 
